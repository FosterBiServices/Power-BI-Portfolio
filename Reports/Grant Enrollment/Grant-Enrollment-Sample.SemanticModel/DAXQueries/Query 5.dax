EVALUATE
	SELECTCOLUMNS(
		TOPN(
			100,
			'Targets',
			'Targets'[ENROLLMENT TARGETS],
			ASC
		),
		'Targets'[ENROLLMENT TARGETS],
		'Targets'[SUB CODE],
		'Targets'[SITE],
		'Targets'[REGION LWDB],
		'Targets'[REGION CODE]
	)
	ORDER BY 'Targets'[ENROLLMENT TARGETS] ASC