EVALUATE
	SELECTCOLUMNS(
		TOPN(
			1000,
			'Regions',
			'Regions'[REGION LWDB],
			ASC
		),
		'Regions'[REGION LWDB],
		'Regions'[REGION CODE],
		'Regions'[REGION SHORT ABBR]
	)
	ORDER BY 'Regions'[REGION LWDB] ASC