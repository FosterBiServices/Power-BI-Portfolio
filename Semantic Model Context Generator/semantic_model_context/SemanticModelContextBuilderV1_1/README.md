# Semantic Model Context Builder 1.1

This release integrates the proven parsing approach from Local TMDL Documenter's `parser.py` and maps its table, column, measure, relationship, model-property, and partition source concepts into the Context Builder domain.

Run tests with `python -m pytest -q`.
